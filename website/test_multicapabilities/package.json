/* Need to start selenium/webdriver first:
   1. `npm install`
   2. `npm run webdriver-start` in a separate terminal
   3. `npm run e2e protractor-chrome.conf`

   But this is not working properly. Probably
   webdriver is not running as expected. */

{
  "name": "mutlicapabilites",
  "description": "Chapter 10 sample code",
  "scripts": {
    "pree2e": "tsc",
    "e2e": "protractor",
    "tsc": "tsc",
    "webdriver-circle": "webdriver-manager update --versions.chrome=2.40",
    "webdriver-update": "webdriver-manager update --standalone false --gecko false",
    "webdriver-start": "webdriver-manager start"
  },
  "author": "Craig Nishina <craig.nishina@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@types/jasmine": "^2.6.0",
    "@types/jasminewd2": "^2.0.2",
    "@types/selenium-webdriver": "^2.53.42",
    "jasmine": "^2.7.0",
    "protractor": "^5.1.2",
    "ts-node": "^3.3.0"
  },
  "devDependencies": {
    "@types/jasminewd2": "2.0.2",
    "@types/selenium-webdriver": "2.53.42",
    "tslint": "^5.5.0",
    "typescript": "^2.4.2"
  }
}
